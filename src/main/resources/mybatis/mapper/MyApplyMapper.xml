<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MyApplyMapper">
	<select id="myApplyTitleList" resultType="com.example.demo.dto.RecruitNoticeDto">
	
	SELECT 
	distinct ta_recruit_notice.recruitNo,ta_recruit_notice.recruitName,ta_recruit_notice.recruitEndDateTime
	FROM 
	ta_recruit_applicant,ta_recruit_notice 
	where ta_recruit_applicant.applicantId= #{memberId}
	and ta_recruit_applicant.recruitNo = ta_recruit_notice.recruitNo;
	
	</select>

	<select id="myApplyTypeList" resultType="com.example.demo.dto.TypeDto">
	select 
	ta_recruit_type.typeName 
	from
	ta_recruit_applicant
	,ta_recruit_type 
	,ta_recruit_notice
	where ta_recruit_applicant.applicantId= #{memberId}
	and ta_recruit_applicant.recruitNo = ta_recruit_notice.recruitNo
	and ta_recruit_type.typeNo = ta_recruit_notice.typeNo;
	
	
	
	</select>
	
	
	<!-- 지원한 이력서가 최종제출되었는 지 확인하는 select문 -->
	<select id = "myFinalApplyCheckedList" resultType="com.example.demo.dto.ApplicantDto">
	select
	*
	from
	ta_recruit_applicant
	where
	ta_recruit_applicant.applicantId= #{memberId}
	
	</select>
	
	<!-- 현재 이력서를 삭제하는 delete문 -->
	<delete id="myDeleteApply">
		delete from ta_recruit_applicant  where applicantId= #{memberId} and recruitNo =#{recruitNo}
	
	
	</delete>
	
</mapper>