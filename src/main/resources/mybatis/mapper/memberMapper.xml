<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MemberMapper">
	<!-- 게시판 글 작성 -->
	<insert id="join">
	    INSERT INTO 
	    	TA_RECRUIT_MEMBER(
	    	   memberName
	    	   , memberBirth
	    	   , memberId
	    	   , memberPw
	    	   , memberPwConfirm
	    	   , memberRoot
	    	   , memberPhoneNumber1
	    	   , memberPhoneNumber2
	    	   , memberPhoneNumber3
	    	   , memberEmailChecked
	    	 )
	         VALUES(
	         	#{memberName} 
	            , #{memberBirth}
	            , #{memberId}
	            , #{memberPw}
	            , #{memberPwConfirm}
	            , #{memberRoot}
	            , #{memberPhoneNumber1}
	            , #{memberPhoneNumber2}
	            , #{memberPhoneNumber3}
	            , 0
	            
	         )
    </insert>
   
   <select id="idJoinCheck" parameterType="String" resultType="int">
   		SELECT (*) 
   				FROM TA_RECRUIT_MEMBER
   				WHERE = MEMBERID = #{MEMBERID}
   			
   
   </select>
  
   
</mapper>